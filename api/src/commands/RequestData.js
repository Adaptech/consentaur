export default class RequestData {
	constructor(dataDescription, dataRequestId, projectId) {
		this.dataDescription = dataDescription;
		this.dataRequestId = dataRequestId;
		this.projectId = projectId;
		
  }
}
