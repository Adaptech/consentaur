export default class CandidateConsented {
  constructor(candidateId, dataRequestId, projectId) {
		this.candidateId = candidateId;
		this.dataRequestId = dataRequestId;
		this.projectId = projectId;
		
  }
}
