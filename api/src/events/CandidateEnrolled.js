export default class CandidateEnrolled {
  constructor(candidateId, email, password, projectId) {
		this.candidateId = candidateId;
		this.email = email;
		this.password = password;
		this.projectId = projectId;
		
  }
}
